{
  "name": "artwall-3.0-contracts",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:baseSepolia": "hardhat run scripts/deploy.ts --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.15",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
